# Manu

A simple web project containing `index.html`, `style.css`, and `script.js`.

## Usage

Open `index.html` in a browser to view the project.

## Files

- `index.html` — main entry point
- `style.css` — styles
- `script.js` — client-side script
